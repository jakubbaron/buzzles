import React, { Component } from 'react';

class Button extends Component {
  render() {
    return (
      <div>
      </div>
    )
  }
}

Button.propTypes = {
  onClick: React.PropTypes.func.isRequred
}
export default Button;